# Chrome Web Store 发布信息（逐项对照填入开发者后台）

> 已核对 v1.6.3 实际代码：权限仅 activeTab / scripting / downloads，无任何网络请求（无 fetch / XHR / WebSocket / eval），图标 16/48/128 齐全。
> 以下审核理由均提供**中文 + 英文**两版；**建议提交英文版**（Google 审核员以英文为主，可加快审核），中文版备用。

---

## 一、隐私权规范标签页（Privacy practices）—— 5 个必填项

### 1. 单一用途说明（Single purpose）

> 要求：一句话说清扩展干什么，不要写成功能清单。

**英文（推荐提交）：**

Web Page Quick Edit lets users visually edit the current web page in the browser — text, images, videos and styles — and export the modified result as a standalone HTML file or PDF.

**中文（备用）：**

让用户在浏览器中直接可视化编辑当前网页（文字、图片、视频与样式），并将修改后的结果导出为 HTML 副本或 PDF。

---

### 2. 远程代码的理由（Remote code justification）

> 本扩展**不使用**任何远程代码。此字段如实声明即可。以下陈述已与代码核实一致。

**英文（推荐提交）：**

This extension does NOT use any remote code. All JavaScript (content.js, background.js) is bundled inside the extension package. At runtime the extension never loads, injects, or evaluates any script from a remote source; all features run entirely locally in the user's browser.

**中文（备用）：**

本扩展不使用任何远程代码。所有 JavaScript（content.js、background.js）均打包在扩展包内，运行时从不从任何远程来源加载、注入或执行脚本，全部功能均在用户浏览器本地完成。

---

### 3. activeTab 权限的理由

> 代码事实：manifest 未声明任何 host_permissions，不申请 <all_urls>；仅在用户点击图标时临时访问当前标签页。

**英文（推荐提交）：**

The extension gains access to the current tab only when the user explicitly clicks the toolbar icon, in order to enable visual editing on that page. It deliberately avoids persistent host permissions (such as <all_urls>), never reads other tabs, and access is cleared when the page is refreshed or the tab is closed — following the principle of least privilege.

**中文（备用）：**

仅在用户主动点击工具栏图标时，扩展才临时获得当前活动标签页的访问权限，用于在该页面开启可视化编辑。扩展刻意不申请 <all_urls> 等常驻主机权限，不读取其他标签页；页面刷新或标签页关闭后访问即失效，符合最小权限原则。

---

### 4. downloads 权限的理由

> 代码事实：仅用 chrome.downloads.download() 保存用户本地编辑生成的 data: URL 文件，从不下载远程资源。

**英文（推荐提交）：**

After the user finishes editing and clicks "Export", the extension uses chrome.downloads to save the user's locally generated modified HTML file (passed as a data: URL) into the browser's default Downloads folder. Every download is content the user created on their own device; the extension never downloads anything from a remote server.

**中文（备用）：**

用户完成编辑并点击「导出副本」后，扩展通过 chrome.downloads 将用户本地生成的修改后 HTML 文件（以 data: URL 形式传递）保存到浏览器默认下载文件夹。所有下载内容均为用户在自己设备上编辑生成，扩展从不从任何远程服务器下载文件。

---

### 5. scripting 权限的理由

> 代码事实：manifest 不含 content_scripts 常驻声明；仅用户点击图标时用 executeScript 注入打包在扩展内的 content.js。

**英文（推荐提交）：**

When the user clicks the toolbar icon to enable edit mode, the extension uses chrome.scripting.executeScript to inject its editor script (content.js, bundled in the package) into the current tab to render the editing UI. Injection is strictly user-initiated, affects only the current tab, and is undone when the user disables edit mode or refreshes the page.

**中文（备用）：**

用户点击工具栏图标开启编辑模式时，扩展使用 chrome.scripting.executeScript 将打包在扩展内的编辑器脚本（content.js）注入当前标签页以呈现编辑界面。注入严格由用户主动触发，仅作用于当前标签页；用户关闭编辑模式或刷新页面后即失效。

---

### 6. 数据使用情况确认（同页底部的确认项）

如实勾选（与代码事实一致，已核实无网络请求）：

- **是否收集用户数据：否**。扩展不收集、不传输、不分享任何用户数据；编辑草稿仅存于浏览器本地 localStorage，导出后即清除
- 身份信息 / 财务信息 / 位置 / 浏览记录 / 网页活动等各项收集声明，**全部选「否」**
- 勾选确认「数据使用情况符合 Chrome Web Store 开发者计划政策」

---

## 二、发布方联系邮箱（设置页面）

这一项必须由你本人操作，插件无法代办：

1. 进入 [Chrome Web Store 开发者后台](https://chrome.google.com/webstore/devconsole) → 右上角账号 → **设置**
2. 在「联系方式」中填写你的邮箱（建议 Gmail 或常用企业邮箱）
3. 点击「验证」→ 到邮箱收验证邮件 → 完成验证
4. 注意：注册开发者账号需一次性支付 **5 美元**（信用卡/借记卡）

---

## 三、商品信息（Store listing，新建商品时填写）

| 字段 | 填写内容 |
|---|---|
| 名称 | 网页随手改 |
| 简短说明（≤132 字符） | 见下方 |
| 详细描述 | 见下方 |
| 类别 | 效率工具（Productivity） |
| 语言 | 中文（简体） |

### 简短说明（132 字符以内）

```
像改 Word 一样改网页：点哪改哪，调字号颜色行距，换图插图，一键导出 HTML/PDF。特别适合修改 AI 生成的 HTML 演示文稿。
```

### 详细描述（可直接粘贴）

```
不会写代码，也能改网页。

「网页随手改」让你在浏览器里直接修改任意网页和 AI 生成的 HTML 演示文稿：点一下就能改，改完一键导出。原始文件不受任何影响。

【它能帮你做什么】

✏️ 点哪改哪
点击页面上的任何文字直接修改。工具栏支持调整字号、文字颜色、行距（细粒度下拉调节）和对齐方式。

🖼️ 换图 / 插图 / 裁剪
鼠标悬停图片出现小工具条：换图、插图、删除、复制。插入的图片支持拖动位置、四角拉伸缩放、框选裁剪（可选压缩质量），还可以插入视频并调整大小位置。

📦 整块移动
页面里的文字模块、卡片可整体拖到新位置，自动锁定宽度，不会出现挪完一行字变两行的问题。

🎨 一键换主题色
自动分析页面配色，列出出现频率最高的 3 个主题色，每个都能单独替换，整页气质一键切换。

🔤 全局换字体
标题、正文分开设置，内置多种版权安全的系统字体。

💾 改坏也不怕
支持撤销 / 重做，编辑草稿自动保存（重新打开页面还在），所有修改只在浏览器里进行，原文件零影响。

📤 一键导出
· 导出 PDF：自动处理多页演示文稿（打印时记得勾选「背景图形」）
· 导出 HTML 副本：
  - 无本地资源依赖：直接导出单文件
  - 引用了 assets 文件夹：可选「单文件模式」（图片字体全部内嵌，发微信邮件直接打开）或「完整文件夹模式」（HTML + 资源整套导出，结构与原来一致）

【适合谁用】

· 需要修改 AI 生成的 HTML 演示文稿的任何人
· 想快速修正网页文案、替换图片的运营 / 编辑
· 需要给网页截图或留档前先美化内容的普通用户

【隐私承诺】

不收集任何数据，不需要注册，不需要联网。所有编辑都在你的浏览器本地完成，导出即清空草稿。

【使用方法】

1. 点击浏览器右上角扩展图标，绿色圆点 ● 表示已开启
2. 点击文字开始编辑，悬停图片出现工具条，底部工具栏调整格式
3. 点击「导出副本」或「导出 PDF」，成品在浏览器默认下载文件夹

打开本地 HTML 文件使用时，需在扩展管理页（chrome://extensions）勾选「允许访问文件网址」。
```

---

## 四、隐私权政策（备用）

> 声明「不收集数据」时通常**不需要**隐私政策；若提交流程仍要求提供隐私权政策 URL（部分情况会出现），把下面内容托管到任意可公开访问的页面（如 GitHub Pages、语雀公开页）即可。

```
网页随手改 隐私政策

「网页随手改」浏览器扩展尊重并保护用户隐私。

我们不收集、不存储、不传输、不分享任何用户个人数据。

具体而言：
· 扩展的所有编辑操作均在用户浏览器本地完成
· 编辑草稿仅保存在浏览器本地（localStorage），导出后自动清除，不会上传到任何服务器
· 扩展不读取浏览记录、不获取用户身份、不使用任何统计或追踪代码
· 扩展运行时无任何网络请求

如对隐私政策有疑问，请通过 Chrome Web Store 页面上的开发者邮箱联系我们。

最后更新：2026年8月
```

---

## 五、打包与提交检查清单

### 打包 zip（只含必需文件，排除 .md）

在终端执行（已排除介绍文案等文档）：

```bash
cd "/Users/shuqianhuiting/Library/Application Support/TRAE SOLO CN/ModularData/ai-agent/work-mode-projects/6a808245f9a165f1b2403eae/html-editor-extension"
zip -r ../网页随手改-v1.6.3.zip manifest.json background.js content.js icons
```

生成的 zip 位于项目根目录，上传这个文件即可。

### 后台提交前的自查

- [x] 图标 16 / 48 / 128 已齐全（在包内 icons/ 目录）
- [ ] 截图：**至少 1 张**，尺寸 1280×800 或 640×400，最多 5 张。建议用 demo 演示文稿页面截：① 开启编辑模式正在改文字 ② 底部工具栏 ③ 主题色面板 ④ 导出面板
- [ ] 宣传图 440×280（可选，建议做，商店展示更醒目）
- [ ] 联系邮箱已填写并验证
- [ ] 隐私权规范 5 项理由全部粘贴（用英文版）
- [ ] 数据使用情况已确认

### 提交后的注意事项

- 审核通常 **1～7 天**，状态在后台可见
- 首次发布可选择可见性：「未列出」（拿到链接的人能用）先自测，再切「公开」全网上架
- 若审核被拒，后台会给出具体原因，最常见的是权限理由描述不清——把英文版理由粘贴过去基本可过
- 后续更新版本：manifest.json 的 version 数字必须大于已发布版本，重新打包上传即可
